# Assignment4
#TASK1
In task 1 we have to read a file and print the data line by line and if the file does not exist we have gracefully show the error using exceptions.
in this program i have used two ways to solve it. Method one with readline one by one and method two with readlines so that all the lines are read at once and pring using a for loop.



#TASK@
in this we have used append + mthod to open the file append as well as read mode together .
after writing some data and appending some data we have to set the file handle to position 0 in the file i.e to set it to the begining of the file so used seek() method and printed the data.
We simply wrote the data using write method and read the data using readlines.
